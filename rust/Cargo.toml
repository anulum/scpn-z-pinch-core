# SPDX-License-Identifier: AGPL-3.0-or-later
# Commercial license available
# © Concepts 1996–2026 Miroslav Šotek. All rights reserved.
# © Code 2020–2026 Miroslav Šotek. All rights reserved.
# ORCID: 0009-0009-3560-0851
# Contact: www.anulum.li | protoscience@anulum.li
# SCPN Z-Pinch Core — native level-0 physics kernels

[package]
name = "scpn-z-pinch-rs"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "AGPL-3.0-or-later"
description = "Native level-0 device-physics kernels of SCPN Z-Pinch Core, bit-exact with the Python floor"
publish = false

[lib]
name = "scpn_z_pinch_native"
crate-type = ["cdylib", "rlib"]

[features]
default = []
python = ["dep:pyo3"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"], optional = true }

[lints.rust]
# Every public item of a native surface carries rustdoc. A binding that
# ships without documentation is debt the next reader pays, so it does not
# ship: the gate is the compiler's, not a review habit.
missing_docs = "deny"
missing_debug_implementations = "deny"
unsafe_code = "deny"

[lints.rustdoc]
broken_intra_doc_links = "deny"
private_intra_doc_links = "deny"
invalid_rust_codeblocks = "deny"

[profile.release]
codegen-units = 1
lto = "fat"
