# SPDX-License-Identifier: AGPL-3.0-or-later
# Commercial license available
# © Concepts 1996–2026 Miroslav Šotek. All rights reserved.
# © Code 2020–2026 Miroslav Šotek. All rights reserved.
# ORCID: 0009-0009-3560-0851
# Contact: www.anulum.li | protoscience@anulum.li
# SCPN Z-Pinch Core — coordinator and stable required gate

name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions: {}

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-policy:
    name: Static analysis and policy
    uses: ./.github/workflows/reusable-static-policy.yml
    permissions:
      contents: read
  tests:
    name: Tests
    uses: ./.github/workflows/reusable-tests.yml
    permissions:
      contents: read
  gate:
    name: Required gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [static-policy, tests]
    if: always()
    permissions: {}
    steps:
      - name: Fail closed on any category result
        env:
          RESULTS: ${{ join(needs.*.result, ' ') }}
        run: |
          for result in $RESULTS; do
            echo "category result: $result"
            if [ "$result" != "success" ]; then
              echo "gate: category did not succeed; failing closed"
              exit 1
            fi
          done
