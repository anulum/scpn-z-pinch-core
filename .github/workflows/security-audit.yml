# SPDX-License-Identifier: AGPL-3.0-or-later
# Commercial license available
# © Concepts 1996–2026 Miroslav Šotek. All rights reserved.
# © Code 2020–2026 Miroslav Šotek. All rights reserved.
# ORCID: 0009-0009-3560-0851
# Contact: www.anulum.li | protoscience@anulum.li
# SCPN Z-Pinch Core — secrets, dependency, licence, and workflow policy

name: Security audit

on:
  push:
  pull_request:
  schedule:
    - cron: "43 5 * * 1"
  workflow_dispatch:

permissions: {}

concurrency:
  group: security-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    name: Secrets, dependencies, licences, workflows
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12.14"
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.24.4"
      - name: Install audit tooling (Go module checksums verify the binaries)
        run: |
          python -m pip install pip-audit==2.10.1 reuse==6.2.0
          go install github.com/zricethezav/gitleaks/v8@v8.30.1
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
      - name: Secret scan
        run: |
          "$(go env GOPATH)/bin/gitleaks" detect --source . --no-banner --redact
      - name: Dependency vulnerability audit
        run: pip-audit -r requirements-dev.txt
      - name: Licensing compliance
        run: reuse lint
      - name: Workflow policy lint
        run: |
          "$(go env GOPATH)/bin/actionlint"
